Kills the other two and starts a new one. Based on its output, it determines if one of those files needs regeneration. Nodes can initially be assigned to files based on the rules chosen by the architect. Wait for the existing tasks to complete before identifying new ones. Use the specific glow/animation that the design intends. The version of autoprefixer used is whatever the compatible version is (check during npm install). The app starts, the CSR should work and pass, etc. The app is an MPA (Multi Page Application) and not a SPA. This file contains the webpack configuration for the project.